Event
| where EventLog == "Microsoft-Windows-DSC/Operational"
| where EventID in (4257, 4106)
| summarize arg_max(TimeGenerated, *) by Computer
| project Computer, Status = case(EventID == 4257, "Compliant", "Error"), Message = RenderedDescription, LastCheck = TimeGenerated
| sort by Status asc
